pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                // Checkout mã nguồn từ Git repository
                git branch: 'main', url: 'git@github.com:khongbach13/apppy.git'
            }
        }
        
    }
}
